// ToDo.txt

General: Set GBA/NDS "mode" and have UI change appropriately: screen border, export, ...
General: Improve when running under Mono (Mac/Linux)
General: Validate object names to ensure they don't collide with generated names.

Undo: Record changing sprite size
Undo: Record undo info for each tab instead of per document
Undo: Record background map edits
Undo: Redo deleted sprite doesn't place sprite in same location in list
Undo: Deleting the default map sprite isn't restored
Undo: bug: duplicate sprite, edit, undo results in blank sprite

Sprite List: disable sprite
Sprite List: reorder sprites (up/down within size group)
Sprite List: scroll to currently selected sprite when selection changes
Sprite List: auto scroll when selecting and mouse moved above/below visible region

Sprite Edit: marquee select
Sprite Edit: tools for line, rect, filled rect, oval filled oval
Sprite Edit: set/manage origin, bounding box
Sprite Edit: cursors for each active tool
Sprite Edit: deleting last sprite in list doesn't select a new sprite

Background Map: flip, rotate tiles, sprites
Background Map: subpalette override
Background Map: collision editor to mark places on map

Background Image: allow transparency color select for 256 color images (all images?)

Background Image List: scroll to currently selected image when selection changes
Background Image List: auto scroll when selecting and mouse moved above/below visible region

Palettes: Add better color editing options: RGB, HSV, large palette
Palettes: Add support for a single 256-color palette

Preferences: Record last save directory
Preferences: Set default GBA/NDS project
Preferences: Store prefs in local xml file instead of in registry

File: catch exceptions when loading to ensure that file is closed properly
File: make file loading robust to poorly formed files (more error/warning messages)
File: don't load into separate document when load. save existing doc and load into main doc.
File: auto-save changes into tmp file

Export: add manifest file for projects
Export: generate project files automatically from manifest
Export: background image: currently only 240x160 images work on GBA. broken on NDS
Export: support export to PA lib

Generated Code: move screen boundary code into tutorial
